<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.bookmanagment">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.AboutUsActivity"/>
        <activity android:name=".Activities.InstructionsActivity"/>
        <activity android:name=".Activities.AddExtraRoomActivity" />
        <activity android:name=".Activities.AddExtraBookActivity" />
        <activity android:name=".Activities.ShelfBookActivity" />
        <activity
            android:name=".Activities.SearchActivity"
            tools:ignore="DuplicateActivity" />
        <activity android:name=".Activities.BookViewerActivity" />
        <activity android:name=".Activities.PDFActivity" />
        <activity android:name=".Activities.LoginPage" />
        <activity android:name=".Activities.SignUpPage" />
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>